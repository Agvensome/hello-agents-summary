# 1.4 在 1.3 节的智能旅行助手基础上，请思考如何添加以下功能（可以只描述设计思路，也可以进一步尝试代码实现）：

```
> **提示**：思考如何修改 `Thought-Action-Observation` 循环来实现这些功能。
-   添加一个"记忆"功能，让智能体记住用户的偏好（如喜欢历史文化景点、预算范围等）
-   当推荐的景点门票已售罄时，智能体能够自动推荐备选方案
-   如果用户连续拒绝了 3 个推荐，智能体能够反思并调整推荐策略
```

1、在代码开头添加记忆存储和状态变量，用于存储用户偏好。
2、收集到对应旅游景点后，先进行门票查询，若门票售罄则重新循环调用大模型提供备选方案，注意循环次数。
3、每次用户选择拒绝，存储当次景点，若连续拒绝3次，自动修改提示词。

---

## 代码实现

完整实现见 src/chapter01/agent_extension.py 。该文件模仿 `agent.py` 的 `Thought-Action-Observation` 循环，复用 `config`、`OpenAICompatibleClient`、`wttrin.get_weather`、`attraction.get_attraction` 四个现有模块。

整体流程对比原版的变化：

```text
原版 agent.py:   LLM思考 -> 调用工具 -> 记录Observation -> (Finish结束)
扩展版:          LLM思考 -> 调用工具 -> 门票检查(售罄则重试) -> 用户确认(接受/拒绝)
                 -> 拒绝计数(满3次修改提示词) -> 记忆注入每轮Prompt -> (Finish结束)
```



### 改动一：记忆功能（记住用户偏好）

**实现步骤：**

1. 在代码开头定义 `memory` 字典作为长期记忆，存储用户偏好（景点类型、预算范围）、已被拒绝的景点名单，以及拒绝/重试计数器。
2. 程序启动时通过 `input()` 收集初始偏好（可直接回车跳过）。
3. 用户接受某个推荐时，再追问其喜欢的特点，追加写入 `memory["user_preferences"]["attraction_types"]`。
4. 每轮循环开始时，通过 `build_memory_str()` 把**最新的**记忆摘要拼接到 Prompt 最前面，让大模型每一轮都能看到用户偏好和拒绝黑名单。

**对应代码：**

记忆存储与状态变量（文件开头）：

```python
memory = {
    "user_preferences": {
        "attraction_types": [],      # 用户喜欢的景点类型, 如 ["历史文化"]
        "budget_range": None,        # 预算范围, 如 "中等"
    },
    "rejected_attractions": [],      # 已被用户拒绝的景点, 避免重复推荐
    "rejection_count": 0,            # 连续拒绝次数
    "current_attraction": None,      # 当前待用户确认的景点
    "ticket_retry_count": 0,         # 门票售罄后的备选重试次数
}

MAX_TICKET_RETRY = 2               # 门票售罄最多重试2次备选
MAX_REJECT_BEFORE_REFLECT = 3      # 连续拒绝3次触发反思
```

记忆格式化与注入（每轮循环构建 Prompt 时执行）：

```python
def build_memory_str() -> str:
    """将记忆内容格式化为可注入Prompt的文本."""
    prefs = memory["user_preferences"]
    types_str = ",".join(prefs["attraction_types"]) if prefs["attraction_types"] else "未知"
    budget_str = prefs["budget_range"] or "未知"
    rejected_str = ",".join(memory["rejected_attractions"]) if memory["rejected_attractions"] else "无"
    return (
        f"[长期记忆] 用户偏好景点类型: {types_str}; 预算范围: {budget_str}; "
        f"已被拒绝的景点(禁止再次推荐): {rejected_str}"
    )

# 主循环 3.1 步:
full_prompt = build_memory_str() + "\n\n" + "\n".join(prompt_history)
```

初始偏好收集：

```python
initial_pref = input("请告诉我您的偏好(如: 喜欢历史文化景点, 预算中等), 直接回车跳过: ").strip()
if initial_pref:
    memory["user_preferences"]["attraction_types"].append(initial_pref)
    print(f"已记住您的偏好: {initial_pref}")
```



### 改动二：门票查询与售罄自动备选

**实现步骤：**

1. 新增模拟工具 `get_ticket_status()`：预置一个售罄名单（欢乐谷/世界之窗/迪士尼），景点名称命中即返回"已售罄"，否则返回"有票"。
2. 将该工具注册进 `available_tools`，并在系统提示词中追加工具说明与规则：**推荐任何景点前必须先调用** `get_ticket_status` **查票**。
3. 主循环中检测到 `get_ticket_status` 返回"已售罄"时，`ticket_retry_count += 1`，并在 Observation 末尾追加系统提示"请改推其他景点"，随后 `continue` 进入下一轮循环，由大模型给出备选方案。
4. 为控制循环次数：售罄重试超过 `MAX_TICKET_RETRY = 2` 次后，Observation 改为强制指令，要求大模型立即用 `Finish[...]` 结束，避免无限重试。
5. 返回"有票"时，先记录门票的 Observation，再暂停进入用户确认环节（见改动三）。

**对应代码：**

模拟门票工具：

```python
def get_ticket_status(attraction: str) -> str:
    """
    模拟门票查询: 预置售罄名单, 景点名称命中即视为售罄.

    :param attraction: 景点名称
    :return: 门票状态
    """
    sold_out_keywords = ["欢乐谷", "世界之窗", "迪士尼"]
    if any(keyword in attraction for keyword in sold_out_keywords):
        return f"门票状态: '{attraction}' 今日门票已售罄"
    return f"门票状态: '{attraction}' 今日有票"
```

系统提示词扩展（追加在 `AGENT_SYSTEM_PROMPT` 之后）：

```python
system_prompt = AGENT_SYSTEM_PROMPT + """
# 新增工具:
- `get_ticket_status(attraction: str)`: 查询指定景点当日门票状态(有票/已售罄)。

# 扩展规则:
- 向用户推荐任何景点前, 必须先调用 get_ticket_status 确认有票; 若Observation提示已售罄, 请改推其他景点
- 用户消息中的[长期记忆]记录了用户偏好与已被拒绝的景点, 推荐时必须参考偏好, 且禁止推荐已被拒绝的景点
"""
```

主循环中的售罄重试逻辑（工具执行之后）：

```python
if tool_name == "get_ticket_status":
    attraction_name = kwargs.get("attraction", "")
    if "已售罄" in observation:
        memory["ticket_retry_count"] += 1
        if memory["ticket_retry_count"] > MAX_TICKET_RETRY:
            observation += (
                f"\n系统提示: 备选重试次数已用完(上限{MAX_TICKET_RETRY}次), "
                f"请立即使用 Action: Finish[...] 告知用户今日景点门票紧张, 无法完成推荐。"
            )
        else:
            observation += (
                f"\n系统提示: 该景点门票已售罄(第{memory['ticket_retry_count']}次售罄), "
                f"请改推其他景点并先查询其门票状态。"
            )
```



### 改动三：用户确认与连续拒绝 3 次反思

**实现步骤：**

1. 门票状态为"有票"时，先判断该景点是否已在 `rejected_attractions` 名单中——若在，则不打扰用户，直接在 Observation 中提示"该景点已被拒绝，请更换"（代码层面兜底"避免重复推荐"）。
2. 否则打印推荐并 `input()` 等待用户输入"接受/拒绝"。
3. **接受**：追问用户喜欢的特点并写入偏好记忆，`rejection_count` 清零，Observation 指示大模型用 `Finish[...]` 输出最终旅行建议。
4. **拒绝**：把该景点追加到 `rejected_attractions`，`rejection_count += 1`，Observation 通知大模型更换推荐。
5. 当 `rejection_count` 达到 `MAX_REJECT_BEFORE_REFLECT = 3` 时，向 `system_prompt` **追加"反思调整"段落**（要求彻底更换推荐思路、严禁重复推荐），随后重置计数器。由于后续每轮调用 LLM 都使用这个修改后的 `system_prompt`，反思策略会持续生效。

**对应代码：**

```python
elif "有票" in observation:
    if attraction_name in memory["rejected_attractions"]:
        # 代码层面兜底: 已被拒绝的景点不再打扰用户
        observation += "\n系统提示: 该景点此前已被用户拒绝, 请勿重复推荐, 请更换其他景点。"
    else:
        # 先记录门票观察结果, 再暂停等待用户确认
        memory["current_attraction"] = attraction_name
        memory["ticket_retry_count"] = 0
        observation_str = f"Observation: {observation}"
        print(f"{observation_str}\n" + "=" * 40)
        prompt_history.append(observation_str)

        print(f"\n>>> 智能体向您推荐景点: {attraction_name}")
        choice = input(">>> 是否接受该推荐? (接受/拒绝): ").strip()
        if choice == "接受":
            pref = input(">>> 您喜欢它的哪类特点? (如: 历史文化/自然风光, 可回车跳过): ").strip()
            if pref:
                memory["user_preferences"]["attraction_types"].append(pref)
                print(f">>> 已记住您的偏好: {pref}")
            memory["rejection_count"] = 0
            observation = (
                f"系统消息: 用户已接受景点 '{attraction_name}' 的推荐。"
                f"请使用 Action: Finish[...] 输出包含天气、推荐景点及门票情况的最终旅行建议。"
            )
        else:
            memory["rejected_attractions"].append(attraction_name)
            memory["rejection_count"] += 1
            print(f">>> 已记录: 拒绝 '{attraction_name}' (连续拒绝 {memory['rejection_count']} 次)")
            if memory["rejection_count"] >= MAX_REJECT_BEFORE_REFLECT:
                # 连续拒绝达到3次: 反思并修改系统提示词, 强制调整推荐策略
                system_prompt += """
# 反思调整(用户已连续拒绝多个推荐):
- 你此前的推荐策略未能满足用户, 请彻底调整思路: 更换景点类型、考虑风格完全不同的备选(如室内/室外切换、免费场馆等)
- 优先参考[长期记忆]中的用户偏好重新匹配, 严禁重复推荐任何已被拒绝的景点
"""
                memory["rejection_count"] = 0
                observation = (
                    f"系统消息: 用户已连续拒绝 {MAX_REJECT_BEFORE_REFLECT} 个推荐, 推荐策略已强制调整。"
                    f"请按照新的系统提示词彻底更换思路, 给出风格明显不同的推荐。"
                )
            else:
                observation = (
                    f"系统消息: 用户拒绝了景点 '{attraction_name}'。"
                    f"请重新推荐其他景点(禁止推荐: {','.join(memory['rejected_attractions'])}), 推荐前务必先查询门票状态。"
                )
```



### 其他细节改动

1. **主循环上限**：从 5 提升到 `MAX_LOOP = 10`（交互确认与门票重试都会消耗循环轮次），并增加 `for...else` 兜底，循环耗尽时打印终止信息。
2. **健壮性解析**：修复了原版 `available_tools[tool_name](*kwargs)` 的解包问题（`*kwargs` 对字典解包得到的是键名），改为 `**kwargs` 关键字传参；对 `Finish[...]` 和工具调用的正则匹配增加了失败兜底，格式错误时通过 Observation 让大模型自我纠正，而不是程序崩溃。



### 验证：

1. 启动后输入偏好（如"喜欢历史文化景点"）或直接回车跳过。
2. 大模型依次查询天气、搜索景点、查询门票。若推荐命中售罄名单（如"欢乐谷"），观察是否自动循环换备选（最多 2 次）。
3. 门票有票时输入"拒绝"3 次，观察第 3 次后打印的"推荐策略已强制调整"，且后续大模型推荐风格发生明显变化、不再重复已拒绝景点。
4. 输入"接受"并填写偏好标签，观察最终 `Finish[...]` 输出，以及偏好是否写入 `memory`。

